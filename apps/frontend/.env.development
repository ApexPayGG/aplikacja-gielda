# Use Vite proxy → http://localhost:3000 (see vite.config.ts)
VITE_API_BASE=/api

# Google Analytics 4 — set your Measurement ID (e.g. G-XXXXXXXXXX). Leave unset to disable GA4.
# VITE_GA_MEASUREMENT_ID=
